pipeline {
    agent any

    stages {
        stage('Echo Message') {
            steps {
                echo 'Hello World!!'
                echo "Build No. : $BUILD_NUMBER"
            }
        }
    }
}